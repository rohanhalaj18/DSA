import java.util.*;

public class Reverse_vowels {
    public static void main(String[] args) {
        String vowels = "APPEIOU";
        char[] a = vowels.toCharArray();
        int left = 0;
        int right = vowels.length() - 1;
        while (right > left) {
            if(a[left]=)
        }
        
        
    }
}
